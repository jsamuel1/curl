name: curl-test # you probably want to 'snapcraft register <name>'
version: '7.58.0+git' # just for humans, typically '1.2+git' or '1.3.2'
summary: CuRL snap # 79 char long summary
description: |
        Curl
grade: devel # must be 'stable' to release into candidate/stable channels
confinement: devmode # use 'strict' once you have the right plugs and slots

parts:
  curl:
          plugin: autotools
          source: .
          prime:
                 - -usr/include
apps:
  curl:
    command: bin/curl
    aliases: 
        - curl
    plugs:
        - network-bind
        - home
